pipeline {
	agent any
	stages {
		stage('Clean') {
		  steps {
			sh 'node --version'
		  }
		}

		stage('build') {
			steps {
				sh 'node --version'
			}
		}

		stage('post-build') {
			steps {
				sh 'node --version'
			}
		}
	}
}