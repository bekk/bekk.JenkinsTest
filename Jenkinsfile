pipeline {
	agent any
	stages {
		stage('Clean') {
		  steps {
			bat 'node --version'
		  }
		}

		stage('build') {
			steps {
				bat 'node --version'
			}
		}

		stage('post-build') {
			steps {
				bat 'node --version'
			}
		}
	}
}